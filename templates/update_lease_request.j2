<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Update Lease Request</title>
</head>
<body>
    <h1>Welcome to the Update Lease Request Page</h1>
    <p>
        <ol>
            <li> Select the Radio button associated with the Field you wish to change</li>
            <li> Enter Into the Text Box the New Desired Value </li>
            <li> Enter the Request ID which you would like to change </li>
            <li> Click "Submit". If Changes are accepted, the Table will be Revised. </li>

        </ol>
    </p>


     <table>
    <! -- This is what establishes the Table -->
    <thead>
        <tr>
            <th>Request ID</th>
            <th>Customer ID</th>
            <th>Derivative ID</th>
            <th>Need Ground Staff</th>
            <th>Need Crew</th>
        </tr>

    <tbody>
    <!-- Now, iterate through every person in bsg_people -->
    {% for person in lease_requests %}
        <tr>
            <!-- Then iterate through every key in the current person dictionary -->
            {% for key in person.keys() %}
            <!-- Create a <td> element with the value of that key in it -->
            <td>{{person[key]}}</td>
            {% endfor %}
        </tr>
    {% endfor %}
    </table>
----------------------------------------

----------------------------------------
<br>UPDATE lease SET<br>
<form action="/update_lease_request" method="post">
    <input type="radio", name="column1" value="request_id" id="request_id">
    <label for="request_id">Request ID</label>
    <br>
    <input type="radio", name="column1" value="customer_id" id="customer_id">
    <label for="customer_id">Customer ID</label>
    <br>
    <input type="radio", name="column1" value="derivative_id" id="derivative_id">
    <label for="derivative_id">Derivative ID</label>
    <br>
    <input type="radio", name="column1" value="need_ground_staff" id="need_ground_staff">
    <label for="need_ground_staff">Need Ground Staff</label>
    <br>
    <input type="radio", name="column1" value="need_crew" id="need_crew">
    <label for="need_crew">Need Crew</label>
    <br>
=
<input type="text" name="value1"><br>
WHERE
<br>
request_id
<br>
=
<input type="number" name="request_id">
<button type="submit">Submit Button</button>
</form>


</body>
</html>