<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Update Lease Request</title>
</head>
<body>
    <h1>Welcome to the Update Lease Request Page</h1>
    <p>
        <ol>
            <li>Update the information in the text Boxes</li>
            <li>When you are pleased with the data, Click "Submit"</li>
            <li>If the refresh page does not display your update, something went wrong</li>
        </ol>
    </p>


     <table>
    <! -- This is what establishes the Table -->
    <thead>
        <tr>
            <th>Request ID</th>
            <th>Customer ID</th>
            <th>Derivative ID</th>
            <th>Need Ground Staff</th>
            <th>Need Crew</th>
        </tr>

    <tbody>
    <!-- Now, iterate through every person in bsg_people -->
    {% for person in data %}
        <tr>
            <!-- Then iterate through every key in the current person dictionary -->
            {% for key in person.keys() %}
            <!-- Create a <td> element with the value of that key in it -->
            <td>{{person[key]}}</td>
            {% endfor %}
        </tr>
    {% endfor %}
        <tr>
            <form action="/lease_request" method="post">
                <td> </td>
                <td><input type="text" name="customer_id" value="{{ data[0]['customer_id'] }}" ></td>
                <td><input type="text" name="derivative_id" value="{{ data[0]['derivative_id'] }}" ></td>
                <td><input type="text" name="need_ground_staff" value="{{ data[0]['need_ground_staff'] }}" ></td>
                <td><input type="text" name="need_crew" value="{{ data[0]['need_crew'] }}"></td>
                <td>
                    <button type="submit" name="update" value="{{ data[0]['request_id'] }}">Submit Updates</button>
                </td>
            </form>
        </tr>
    </table>
----------------------------------------

----------------------------------------

</body>
</html>