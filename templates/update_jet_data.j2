<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Update Jet Data</title>
</head>
<body>
    <h1>Welcome to the Update Jet Data Page</h1>
    <p>
        <ol>
            <li>Update the information in the text Boxes</li>
            <li>When you are pleased with the data, Click "Submit"</li>
            <li>If the refresh page does not display your update, something went wrong</li>
        </ol>
    </p>


     <table>
    <! -- This is what establishes the Table -->
    <thead>
        <tr>
            <th>Jet ID</th>
            <th>Derivative ID</th>
            <th>Number of Engines</th>
            <th>Flight Cycles</th>
            <th>Market Value</th>
            <th>Payload</th>
        </tr>

    <tbody>
    <!-- Now, iterate through every person in bsg_people -->
    {% for person in jets %}
        <tr>
            <!-- Then iterate through every key in the current person dictionary -->
            {% for key in person.keys() %}
            <!-- Create a <td> element with the value of that key in it -->
            <td>{{person[key]}}</td>
            {% endfor %}
        </tr>
    {% endfor %}
        <tr>
            <form action="/jet_data" method="post">
                <td> </td>
                <td><input type="text" name="derivative_id" value="{{ jets[0]['derivative_id'] }}"></td>
                <td><input type="text" name="num_engine" value="{{ jets[0]['num_engine'] }}" ></td>
                <td><input type="text" name="flight_cycle" value="{{ jets[0]['flight_cycle'] }}"></td>
                <td><input type="text" name="market_value" value="{{ jets[0]['market_value'] }}"></td>
                <td><input type="text" name="payload" value="{{ jets[0]['payload'] }}"></td>
                <td>
                    <button type="submit" name="update" value="{{ jets[0]['jet_id'] }}">Submit Updates</button>
                </td>

            </form>
        </tr>
    </table>
----------------------------------------

----------------------------------------
</body>
</html>