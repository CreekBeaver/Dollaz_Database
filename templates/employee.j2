<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Employee</title>
</head>
<body>
    <h1>Welcome to the Employee Page</h1>
    <p>

        <ul>
            <li> View, Insert, and Delete Values from the Employee Table Below</li>
            <li> Click the "Update" Button to Update an Existing Row</li>
            <li> Apply Filters to Change the View of the Table Below</li>
        </ul>
    </p>


     <table>
    <! -- This is what establishes the Table -->
    <thead>
        <tr>
            <th>Employee ID</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Employment Start Date</th>
            <th>Employment End Date</th>
            <th>Title</th>
            <th>Salary ($)</th>
        </tr>
        <form action="/employee" method="post">
            <tr>
                {% for key in data[0].keys() %}
                <!-- Create a <th> tag with the key inside of it, this will be our header row -->
                <th><input type="text" name="{{ key }}"></th>
                <!-- End of this for loop -->
                {% endfor %}
                <th><button type="submit" name="add_row" value="execute">Add Row</button></th>
            </tr>
        </form>

    </thead>
    <tbody>

    <!-- Now, iterate through every person in bsg_people -->
    {% for thing in data %}
        <tr>
    <!-- Then iterate through every key in the current person dictionary -->
    {% for key in thing.keys() %}

    <!-- Create a <td> element with the value of that key in it -->
            <td>{{thing[key]}}</td>
    {% endfor %}
            <td>
                <form action="/employee" method="post">
                <button type="submit" name="delete", value="{{ thing['employee_id'] }}">Delete</button>
                </form>
            </td>
            <td>
                <form action="/update_employee" method="get">
                <button type="submit" name="update" value="{{ thing['employee_id'] }}">Update</button>
                </form>
            </td>
        </tr>
    {% endfor %}

    </tbody>
    </table>
--------------------------------------
    <p>

    </p>
    <p> Apply Filters Here</p>
    <form action="'/employee" method="post">
        <input type="checkbox" id="employee_id" value="employee_id">
        <label for="employee_id">Employee ID</label>
        <br>
        <input type="checkbox" id="first_name" value="first_name">
        <label for="first_name">First Name</label>
        <br>
        <input type="checkbox" id="last_name" value="last_name">
        <label for="last_name">Last Name</label>
        <br>
        <input type="checkbox" id="employment_start_date" value="employment_start_date">
        <label for="employment_start_date">Employment Start Date</label>
        <br>
        <input type="checkbox" id="employment_end_date" value="employment_end_date">
        <label for="employment_end_date">Employment End Date</label>
        <br>
        <input type="checkbox" id="title" value="title">
        <label for="employee_id">Title</label>
        <br>
        <input type="checkbox" id="salary" value="salary">
        <label for="salary">Salary</label>
        <br>
        <button type="submit" name="filter", value="filter">Apply Filter</button>
    </form>

    -----------------------------------------
</body>
</html>