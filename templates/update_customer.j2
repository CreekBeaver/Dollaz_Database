<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Customers</title>
</head>
<body>
    <h1>Welcome to the Update Customer Page</h1>
    <p>
        <ol>
            <li> Select the Radio button associated with the Field you wish to change</li>
            <li> Enter Into the Text Box the New Desired Value </li>
            <li> Enter the Customer ID which you would like to change </li>
            <li> Click "Submit". If Changes are accepted, the Table will be Revised. </li>

        </ol>
    </p>


     <table>
    <! -- This is what establishes the Table -->
    <thead>
        <tr>
            <th>Customer ID</th>
            <th>Name</th>
            <th>Contact Number</th>
            <th>Address</th>
        </tr>

    <tbody>
    <!-- Now, iterate through every person in bsg_people -->
    {% for person in customers %}
        <tr>
            <!-- Then iterate through every key in the current person dictionary -->
            {% for key in person.keys() %}
            <!-- Create a <td> element with the value of that key in it -->
            <td>{{person[key]}}</td>
            {% endfor %}
        </tr>
    {% endfor %}
    </table>
----------------------------------------

----------------------------------------
<br>UPDATE customer SET<br>
<form action="/update_customer" method="post">
    <input type="radio", name="column1" value="name" id="name">
    <label for="name">Customer Name</label>
    <br>
    <input type="radio", name="column1" value="contact_num" id="contact_num">
    <label for="contact_num">Contact Number</label>
    <br>
    <input type="radio", name="column1" value="address" id="address">
    <label for="address">Address</label>
    <br>

=
<input type="text" name="value1"><br>
WHERE
<br>
customer_id
<br>
=
<input type="number" name="customer_id">
<button type="submit">Submit Button</button>
</form>


</body>
</html>