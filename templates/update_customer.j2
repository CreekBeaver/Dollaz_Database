<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Update Customers</title>
</head>
<body>
    <h1>Welcome to the Update Customer Page</h1>
    <p>
        <ol>
            <li>Update the information in the text Boxes</li>
            <li>When you are pleased with the data, Click "Submit"</li>
            <li>If the refresh page does not display your update, something went wrong</li>
        </ol>
    </p>


     <table>
    <! -- This is what establishes the Table -->
    <thead>
        <tr>
            <th>Customer ID</th>
            <th>Name</th>
            <th>Contact Number</th>
            <th>Address</th>
        </tr>

    <tbody>
    <!-- Now, iterate through every person in bsg_people -->
    {% for person in customers %}
        <tr>
            <!-- Then iterate through every key in the current person dictionary -->
            {% for key in person.keys() %}
            <!-- Create a <td> element with the value of that key in it -->
            <td>{{person[key]}}</td>
            {% endfor %}
        </tr>
    {% endfor %}
      <tr>
            <form action="/customer" method="post">
                <td> </td>
                <td><input type="text" name="name" value="{{ customers[0]['name'] }}" ></td>
                <td><input type="text" name="contact_number" value="{{ customers[0]['contact_num'] }}" ></td>
                <td><input type="text" name="address" value="{{ customers[0]['address'] }}" ></td>
                <td>
                    <button type="submit" name="update" value="{{ customers[0]['customer_id'] }}">Submit Updates</button>
                </td>

            </form>
        </tr>
    </table>




</body>
</html>