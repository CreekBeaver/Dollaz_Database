<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Employess</title>
</head>
<body>
    <p>Employees</p>


     <table>
    <! -- This is what establishes the Table -->
    <thead>
        <tr>
        <!-- Iterate through each key in the first entry to get the column name -->
        {% for key in employees[0].keys() %}

        <!-- Create a <th> tag with the key inside of it, this will be our header row -->
        <th>{{ key }}</th>

        <!-- End of this for loop -->
         {% endfor %}
        </tr>
    <tbody>

    <!-- Now, iterate through every person in bsg_people -->
    {% for person in employees %}
        <tr>
    <!-- Then iterate through every key in the current person dictionary -->
    {% for key in person.keys() %}

    <!-- Create a <td> element with the value of that key in it -->
            <td>{{person[key]}}</td>
    {% endfor %}
        </tr>
    {% endfor %}
    </table>

    ----------------------------------------
<br>UPDATE employees SET<br>
<form action="/update_employees" method="post">
    <!-- Iterate through each key in the first entry to get the column name -->
    {% for key in employees[0].keys() %}
    <input type="radio", name="column1" value="{{ key }}" id="{{ key }}">
    <label for="{{ key }}">{{ key }}</label>
    <br>
<!-- End of this for loop -->
    {% endfor %}
=
<input type="text" name="value1"><br>
Where
<br>
employee_id
<br>
=
<input type="text" name="value2">
<button type="submit">Submit Button</button>
</form>


</body>
</html>