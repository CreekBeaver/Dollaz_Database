<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Update Derivative Data</title>
</head>
<body>
    <h1>Welcome to the Update Derivative Data Page</h1>
    <p>
        <ol>
            <li> Select the Radio button associated with the Field you wish to change</li>
            <li> Enter Into the Text Box the New Desired Value </li>
            <li> Enter the Derivative ID which you would like to change </li>
            <li> Click "Submit". If Changes are accepted, the Table will be Revised. </li>

        </ol>
    </p>


     <table>
    <! -- This is what establishes the Table -->
    <thead>
        <tr>
            <th>Derivative ID</th>
            <th>Model Derivative</th>
            <th>Body Style</th>
            <th>Primary Use</th>
            <th>Flight Range (Miles)</th>
            <th>Max Seating</th>
            <th>Fuel Efficiency (gallons / mile)</th>
            <th>Max Takeoff Weight (lbs)</th>
        </tr>

    <tbody>
    <!-- Now, iterate through every person in bsg_people -->
    {% for person in data %}
        <tr>
            <!-- Then iterate through every key in the current person dictionary -->
            {% for key in person.keys() %}
            <!-- Create a <td> element with the value of that key in it -->
            <td>{{person[key]}}</td>
            {% endfor %}
        </tr>
    {% endfor %}
    </table>
----------------------------------------

----------------------------------------
<br>UPDATE employee SET<br>
<form action="/update_derivative_data" method="post">
    <input type="radio", name="column1" value="model_derivative" id="model_derivative">
    <label for="model_derivative">Model Derivative</label>
    <br>
    <input type="radio", name="column1" value="body_style" id="body_style">
    <label for="body_style">Body Style</label>
    <br>
    <input type="radio", name="column1" value="primary_use" id="primary_use">
    <label for="primary_use">Primary Use</label>
    <br>
    <input type="radio", name="column1" value="flight_range" id="flight_range">
    <label for="flight_range">Flight Range</label>
    <br>
    <input type="radio", name="column1" value="max_seating" id="max_seating">
    <label for="max_seating">Max Seating</label>
    <br>
    <input type="radio", name="column1" value="fuel_efficiency" id="fuel_efficiency">
    <label for="fuel_efficiency">Fuel Efficiency</label>
    <br>
    <input type="radio", name="column1" value="max_takeoff_weight" id="max_takeoff_weight">
    <label for="max_takeoff_weight">MTOW</label>
    <br>
=
<input type="text" name="value1"><br>
WHERE
<br>
derivative_id
<br>
=
<input type="number" name="derivative_id">
<button type="submit">Submit Button</button>
</form>


</body>
</html>